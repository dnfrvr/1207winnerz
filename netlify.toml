# Si un netlify.toml existe déjà à la racine du repo, ajoute juste ce bloc [functions]
# dedans (ne duplique pas tout le fichier).
[functions]
  directory = "netlify/functions"

[build]
  # Adapte si ta commande/dossier de build sont différents.
  publish = "dist"
